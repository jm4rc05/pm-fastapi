type Resource {
    id: ID!
    name: String!
    description: String!
}

type Query{
    resource: Resource,
    resources: [Resource!]!
}

type Mutation {
    add(
        name: String!,
        description: String!
    ): Resource!
}
