type Person {
    id: ID!
    name: String!
    title: String!
}

type Query{
    person: Person,
    persons: [Person!]!
}

type Mutation {
    add(
        name: String!,
        title: String!
    ): Person!
}
